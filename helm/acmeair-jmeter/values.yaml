# Default values for acmeair.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

acmeairHost: acmeair.apps.sandbox.power.mpl
acmeairJmeterImage: image-registry.openshift-image-registry.svc:5000/acmeair/acmeair-jmeter
registryTLSverify: false

pipeline:
  enabled: false